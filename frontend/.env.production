# Production: backend phục vụ luôn frontend nên hai bên cùng origin.
# Để đường dẫn tương đối, không hardcode domain - đổi host không cần build lại.
VITE_API_URL=/api

# Để trống nghĩa là Socket.IO kết nối tới chính origin đang mở trang.
VITE_SOCKET_URL=
